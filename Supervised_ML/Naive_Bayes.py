"""
Naive Bayes classifiers are essentially machine learning models that accept text as
input and generate a probabiltity to classify the data. We could write it as:
                  P(spam | message)
The expression above basically means 
"""